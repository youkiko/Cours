// initialise une variable (var ou let ou const)
// en général, on utilise let
let test = 0; 

console.log(test);

// donne une valeur à la variable
// la bonne pratique voudrait qu'on donne une valeur dès le départ (même une de défaut: 0 ou une chaine de caractère vide)
test = 56;

console.log(test); 