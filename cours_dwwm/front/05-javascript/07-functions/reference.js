// mutation du tableau ou un nouveau tableau

// const newTab = tab.map((val) => val * 2);
// const newTab = tab.reduce((a, b) => a * b, 1);
// const newTab = tab.slice(2)
// tab.reverse(); //tableau d'origine référencé 

let tab = [56,78,57,89];
// const newTab = tab.filter((nombre) => nombre > 70)
const verif = tab.includes(53);
console.log(verif);

// console.log(tab);
// console.log(newTab);